<%= form_for(@shopping_trip) do |f| %>
  <% if @shopping_trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shopping_trip.errors.count, "error") %> prohibited this shopping_trip from being saved:</h2>

      <ul>
      <% @shopping_trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :integer %><br />
    <%= f.text_field :integer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
